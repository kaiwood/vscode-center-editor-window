{
    "name": "center-editor-window",
    "displayName": "Center Editor Window",
    "description": "Does exactly what the title suggests",
    "version": "0.0.1",
    "publisher": "kaiwood",
    "engines": {
        "vscode": "^1.4.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:extension.centerEditorWindow"
    ],
    "main": "./out/src/extension",
    "contributes": {
        "commands": [{
            "command": "extension.centerEditorWindow",
            "title": "Center Editor Window"
        }]
    },
    "scripts": {
        "vscode:prepublish": "node ./node_modules/vscode/bin/compile",
        "compile": "node ./node_modules/vscode/bin/compile -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install"
    },
    "devDependencies": {
        "typescript": "^1.8.5",
        "vscode": "^0.11.0"
    }
}